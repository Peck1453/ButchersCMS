@model IEnumerable<Butchers.Data.BEANS.ProductItemBEAN>

@* Setting the viewbag here means that this can be called in the partial view, meaning the name can be changed depending on the page. *@
@{
    ViewBag.Title = "Product Items (Active)";
}

@* Renders the partial view which uses a standard layout for product items *@
@* This allows for the reuse of code *@
@{
    Html.RenderPartial("_ProductItems");
}


