<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - Butchers</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")

</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="#">Meat Management</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarColor01">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item active">
                        @Html.ActionLink("Home", "Index", "Home")
                    </li>
                    <li class="nav-item">
                        @Html.ActionLink("Meats", "Meats", "Product")</a>
                    </li>
                    <li class="nav-item">
                        @Html.ActionLink("Products", "Products", "Product")
                    </li>
                    <li class="nav-item">
                        @Html.ActionLink("Product Items", "ProductItems", "Product")
                    </li>
                    <li class="nav-item">
                        @Html.ActionLink("Promo Codes", "PromoCode", "Order")
                    </li>
                    <li class="nav-item">
                        @Html.ActionLink("Cart Items", "CartItems", "Order")
                    </li>
                    <li class="nav-item">
                        @Html.ActionLink("Orders", "Orders", "Order")
                    </li>
                </ul>
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item">
                        @Html.Partial("_LoginPartial")
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <div class="container body-content">
        @RenderBody()
        <hr />
        <footer>
            <p style="text-align: center;">Sheffield Hallam University (2017/18) - MSc Computing: SDDP Assignment</p>
            <p style="text-align: center;">&copy; Andy's Meat Solution (@DateTime.Now.Year)</p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @RenderSection("scripts", required: false)
</body>
</html>
